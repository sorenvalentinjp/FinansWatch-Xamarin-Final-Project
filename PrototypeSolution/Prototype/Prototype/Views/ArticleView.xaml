<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:Prototype.Views.CustomRenderers"
             x:Class="Prototype.Views.ArticleView"
             BindingContext="{Binding Article}"
             Title="{Binding HomeSectionName}">

    <ContentPage.ToolbarItems>
        <ToolbarItem Text="ALL"/>
    </ContentPage.ToolbarItems>

    <ScrollView>
        <StackLayout>

            <Image Source="{Binding ImageSourceBig}"
                   Aspect="AspectFill"
                   HeightRequest="200"/>
            <Label Text="{Binding ImageCaption}" Margin="10,0,30,0" FontSize="Small"/>
            <Label Text="{Binding Title}" Margin="15,10,30,0" FontSize="Large" FontAttributes="Bold"/>
            <local:HtmlFormattedLabel Text="{Binding Teaser}" Margin="15,10,30,0" FontSize="Medium" FontAttributes="Bold"/>
            <Label Text="{Binding PublishInfo}" Margin="15,10,30,0" FontSize="Small"/>
            <local:HtmlFormattedLabel Text="{Binding BodyText}" Margin="20,15,20,10" FontSize="16"/>

        </StackLayout>
    </ScrollView>

</ContentPage>