<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:Prototype.Views.CustomRenderers"
             x:Class="Prototype.Views.ArticleView"
             BindingContext="{Binding Article}"
             Title="SEKTION HER!!!">

    <ContentPage.ToolbarItems>
        <ToolbarItem Text="ALL"/>
    </ContentPage.ToolbarItems>

    <ScrollView>
        <StackLayout>

            <Image Source="{Binding ImageSourceBig}"/>
            <Label Text="{Binding ImageCaption}" Margin="10"/>
            <Label Text="{Binding Title}" Margin="20"/>
            <local:HtmlFormattedLabel Text="{Binding Teaser}" Margin="20"/>
            <Label Text="{Binding PublishInfo}" Margin="20"/>
            <local:HtmlFormattedLabel Text="{Binding BodyText}" Margin="30"/>

        </StackLayout>
    </ScrollView>

</ContentPage>