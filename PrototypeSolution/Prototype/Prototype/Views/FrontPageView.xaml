<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:Prototype.Views"
             xmlns:customRenderers="clr-namespace:Prototype.Views.CustomRenderers"
             x:Class="Prototype.Views.FrontPageView"
             Title="FORSIDE">
    
    <!--Setting up resources for the view-->
    <ContentPage.Resources>
        <ResourceDictionary>
            <DataTemplate x:Key="topArticleTemplate">
                <ViewCell>
                    <ViewCell.View>

                        <StackLayout Orientation="Vertical">
                            <Image Source="{Binding ImageSourceBig}"
                                   HorizontalOptions="FillAndExpand"
                                   HeightRequest="200"
                                   Aspect="AspectFill"/>
                            <StackLayout Orientation="Vertical"
                                         Padding="10,10,0,0">
                                <Label Text="{Binding Title}"
                                       HorizontalOptions="FillAndExpand"
                                       Font="Bold, 20"/>
                                <customRenderers:HtmlFormattedLabel Text="{Binding Teaser}"
                                                                    Font="15"/>
                            </StackLayout>
                        </StackLayout>
                    </ViewCell.View>
                </ViewCell>
            </DataTemplate>

            <DataTemplate x:Key="articlesTemplate">
                <ViewCell>
                    <ViewCell.View>
                        <StackLayout Orientation="Horizontal" 
                                     Padding="10">
                            <Image Source="{Binding ImageSourceSmall}" 
                                   Aspect="AspectFit"
                                   HeightRequest="70"
                                   WidthRequest="100"
                                   />
                            <Label Text="{Binding Title}"
                                   Font="15"
                                   />
                        </StackLayout>
                    </ViewCell.View>
                </ViewCell>
            </DataTemplate>

            <!--HorizontalOptions="StartAndExpand"
                                   HeightRequest="70"
                                   Aspect="AspectFill"-->
            
            <local:ArticleTemplateSelector x:Key="articleTemplateSelector" TopArticleTemplate="{StaticResource topArticleTemplate}" ArticlesTemplate="{StaticResource articlesTemplate}"/>
            
        </ResourceDictionary>
    </ContentPage.Resources>

    <!--Adds the button at the navigation bar-->
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="ALL"/>
    </ContentPage.ToolbarItems>

    <!--This view only contains a listview-->
    <ContentPage.Content>
        <ListView x:Name="listView" 
                  ItemTemplate="{StaticResource articleTemplateSelector}" 
                  ItemsSource="{Binding Articles}" 
                  IsPullToRefreshEnabled="True"
                  RefreshCommand="{Binding RefreshCommand}"
                  IsRefreshing="{Binding IsRefreshing}"
                  HasUnevenRows="True"
                  HeightRequest="200"/>
    </ContentPage.Content>

</ContentPage>