<?xml version="1.0" encoding="utf-8" ?>
<mr:ViewCell xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:mr="clr-namespace:MR.Gestures;assembly=MR.Gestures"
             xmlns:ffimageloading="clr-namespace:FFImageLoading.Forms;assembly=FFImageLoading.Forms"
             x:Class="Prototype.Views.Cells.MediumCell"
             Tapped="TappedGesture"
             LongPressing="LongPressingGesture">

    <ViewCell.View>
        <Grid Margin="10,10,10,10">
            <Grid.RowDefinitions>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <ffimageloading:CachedImage x:Name="image"
                                        Grid.Row="0" 
                                        Grid.Column="0" 
                                        LoadingPlaceholder="{Binding ImageTransparentSmall}"
                                        Source="{Binding FrontPageImage.SmallUrl}"
                                        HorizontalOptions="StartAndExpand"/>
            <Label Text="{Binding Title}" 
                   Grid.Row="0" 
                   Grid.Column="1" 
                   Grid.ColumnSpan="2"
                   Font="16"/>
        </Grid>
    </ViewCell.View>
</mr:ViewCell>